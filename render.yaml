services:
  - type: web
    name: amway-speech-ai
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: VALID_API_KEY
        sync: false
      - key: API_AUTH_HEADER
        sync: false
      - key: GEMINI_API_KEY
        sync: false